# Asset Validator

A Blender 5.x addon for validating 3D assets against game-engine import requirements.

The current release is a loadable skeleton. It adds an **Asset Validator** tab to
the 3D Viewport sidebar with a placeholder **Run Validation** button.
