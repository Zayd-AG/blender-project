# Import Validator Studio Plugin

The Roblox Studio companion to Asset Validator. It provides an **Import Validator** toolbar button that opens a docked panel with a placeholder **Scan Selection** action.

This skeleton intentionally contains no validation logic and does not yet read the shared naming pattern. Use Rojo 7.5.1 to build or live-sync it during development.
